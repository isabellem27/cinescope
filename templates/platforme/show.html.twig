{% extends 'base.html.twig' %}

{% block title %}Platforme{% endblock %}

{% block body %}
    <h1>Platforme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ platforme.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ platforme.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ platforme.url }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ platforme.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_platforme_index') }}">back to list</a>

    <a href="{{ path('app_platforme_edit', {'id': platforme.id}) }}">edit</a>

    {{ include('platforme/_delete_form.html.twig') }}
{% endblock %}
